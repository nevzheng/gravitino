# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.

# coding: utf-8

"""
    Apache Gravitino V1 REST API

    The authoritative contract for the versioned Apache Gravitino V1 API.  V1 is selected only by the `/api/v1` route. Clients send and receive `application/json`; vendor media types and version-selection headers are not part of this contract. The unversioned `/api` API is a separate legacy surface and is not described here.  V1 JSON mutation bodies on `POST` and `PUT` must declare `Content-Type: application/json`. Standard media-type parameters, such as `charset`, are accepted; an absent, ambiguous, malformed, or other media type, including `application/*+json`, receives `415 UNSUPPORTED_MEDIA_TYPE` before authentication.  Optional resource properties are omitted when no value exists. JSON `null` is serialized only where a schema explicitly permits it, such as an Iceberg `NULL` literal. Required collections and maps are serialized as empty arrays or objects when they contain no entries. Response object schemas explicitly allow additional properties: clients must ignore members they do not understand. Adding an optional response property is a compatible minor V1 contract release; removing a property, changing its type or meaning, or making an optional property required needs a new routed major version. Request object schemas use separate closed schemas and may reject unknown properties.  Every operation also documents a generic `500 INTERNAL_ERROR` fallback. It means only that an unclassified server failure was sanitized at the public boundary; it is not an expected product outcome or a supported diagnosis. Expected failures use their specific HTTP status and public error type. Clients use the returned request ID for support and do not retry `INTERNAL_ERROR` automatically.  Authentication is deployment-configured. When enabled, credentials use the standard [`Authorization` header](https://www.rfc-editor.org/rfc/rfc9110#section-11.6.2). This base contract deliberately does not declare it as a [Parameter Object](https://spec.openapis.org/oas/v3.1.2.html#parameter-object): OpenAPI ignores parameters with that name. A deployment-specific profile must instead declare its exact [Security Scheme](https://spec.openapis.org/oas/v3.1.2.html#security-scheme-object) and [Security Requirement](https://spec.openapis.org/oas/v3.1.2.html#security-requirement-object), including its authentication challenge behavior. This permits Basic, Bearer, Negotiate, custom, or anonymous Gravitino deployments without falsely claiming one universal scheme.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from gravitino_client.models.apply_transform import ApplyTransform
from gravitino_client.models.bucket_transform import BucketTransform
from gravitino_client.models.list_transform import ListTransform
from gravitino_client.models.range_transform import RangeTransform
from gravitino_client.models.single_field_transform import SingleFieldTransform
from gravitino_client.models.truncate_transform import TruncateTransform
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

PARTITIONTRANSFORM_ONE_OF_SCHEMAS = ["ApplyTransform", "BucketTransform", "ListTransform", "RangeTransform", "SingleFieldTransform", "TruncateTransform"]

class PartitionTransform(BaseModel):
    """
    A physical table partition transform.
    """
    # data type: SingleFieldTransform
    oneof_schema_1_validator: Optional[SingleFieldTransform] = None
    # data type: BucketTransform
    oneof_schema_2_validator: Optional[BucketTransform] = None
    # data type: TruncateTransform
    oneof_schema_3_validator: Optional[TruncateTransform] = None
    # data type: ListTransform
    oneof_schema_4_validator: Optional[ListTransform] = None
    # data type: RangeTransform
    oneof_schema_5_validator: Optional[RangeTransform] = None
    # data type: ApplyTransform
    oneof_schema_6_validator: Optional[ApplyTransform] = None
    actual_instance: Optional[Union[ApplyTransform, BucketTransform, ListTransform, RangeTransform, SingleFieldTransform, TruncateTransform]] = None
    one_of_schemas: Set[str] = { "ApplyTransform", "BucketTransform", "ListTransform", "RangeTransform", "SingleFieldTransform", "TruncateTransform" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = PartitionTransform.model_construct()
        error_messages = []
        match = 0
        # validate data type: SingleFieldTransform
        if not isinstance(v, SingleFieldTransform):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SingleFieldTransform`")
        else:
            match += 1
        # validate data type: BucketTransform
        if not isinstance(v, BucketTransform):
            error_messages.append(f"Error! Input type `{type(v)}` is not `BucketTransform`")
        else:
            match += 1
        # validate data type: TruncateTransform
        if not isinstance(v, TruncateTransform):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TruncateTransform`")
        else:
            match += 1
        # validate data type: ListTransform
        if not isinstance(v, ListTransform):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ListTransform`")
        else:
            match += 1
        # validate data type: RangeTransform
        if not isinstance(v, RangeTransform):
            error_messages.append(f"Error! Input type `{type(v)}` is not `RangeTransform`")
        else:
            match += 1
        # validate data type: ApplyTransform
        if not isinstance(v, ApplyTransform):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ApplyTransform`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in PartitionTransform with oneOf schemas: ApplyTransform, BucketTransform, ListTransform, RangeTransform, SingleFieldTransform, TruncateTransform. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in PartitionTransform with oneOf schemas: ApplyTransform, BucketTransform, ListTransform, RangeTransform, SingleFieldTransform, TruncateTransform. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into SingleFieldTransform
        try:
            instance.actual_instance = SingleFieldTransform.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into BucketTransform
        try:
            instance.actual_instance = BucketTransform.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into TruncateTransform
        try:
            instance.actual_instance = TruncateTransform.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ListTransform
        try:
            instance.actual_instance = ListTransform.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into RangeTransform
        try:
            instance.actual_instance = RangeTransform.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ApplyTransform
        try:
            instance.actual_instance = ApplyTransform.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into PartitionTransform with oneOf schemas: ApplyTransform, BucketTransform, ListTransform, RangeTransform, SingleFieldTransform, TruncateTransform. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into PartitionTransform with oneOf schemas: ApplyTransform, BucketTransform, ListTransform, RangeTransform, SingleFieldTransform, TruncateTransform. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], ApplyTransform, BucketTransform, ListTransform, RangeTransform, SingleFieldTransform, TruncateTransform]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
